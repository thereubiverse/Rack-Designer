# kong.yml — declarative Kong config, templated at container start.
#
# Shape modeled on Supabase's own self-hosting config
# (github.com/supabase/supabase, docker/docker-compose.yml + docker/volumes/api/kong.yml), trimmed
# to only the three services this stack actually runs: auth (GoTrue), rest (PostgREST) and storage.
# The realtime/meta/studio routes present in the upstream file are deliberately not reproduced here
# — see spec §3 for why those services are absent entirely.
#
# ${ANON_KEY} / ${SERVICE_ROLE_KEY} below are NOT resolved by Kong's declarative-config loader (it
# does not do environment substitution). The `kong` service's entrypoint in ../docker-compose.yml
# shells out to `eval "echo \"$(cat temp.yml)\""` before Kong starts, which is what turns this file
# into a concrete config with the real JWTs from deploy/.env spliced in.

_format_version: "2.1"
_transform: true

consumers:
  - username: anon
    keyauth_credentials:
      - key: ${ANON_KEY}
  - username: service_role
    keyauth_credentials:
      - key: ${SERVICE_ROLE_KEY}

acls:
  - consumer: anon
    group: anon
  - consumer: service_role
    group: admin

services:
  - name: auth-v1
    _comment: "GoTrue: /auth/v1/* -> http://auth:9999/*"
    url: http://auth:9999/
    routes:
      - name: auth-v1-all
        strip_path: true
        paths:
          - /auth/v1/
    plugins:
      - name: cors
      - name: key-auth
        config:
          hide_credentials: true
      - name: acl
        config:
          hide_groups_header: true
          allow:
            - admin
            - anon

  - name: rest-v1
    # Publicly reachable only because migrations 0027/0028 revoked anon's and authenticated's
    # privileges on `public` down to a single column grant — see
    # src/lib/supabase/grants.test.ts. A found /rest/v1 endpoint is not a database.
    _comment: "PostgREST: /rest/v1/* -> http://rest:3000/*"
    url: http://rest:3000/
    routes:
      - name: rest-v1-all
        strip_path: true
        paths:
          - /rest/v1/
    plugins:
      - name: cors
      - name: key-auth
        config:
          hide_credentials: true
      - name: acl
        config:
          hide_groups_header: true
          allow:
            - admin
            - anon

  - name: storage-v1
    # Public on purpose: signed storage URLs are fetched directly by the browser (avatars, floor
    # plans). See ../Caddyfile for the full reasoning.
    _comment: "Storage: /storage/v1/* -> http://storage:5000/*"
    url: http://storage:5000/
    routes:
      - name: storage-v1-all
        strip_path: true
        paths:
          - /storage/v1/
    plugins:
      - name: cors
