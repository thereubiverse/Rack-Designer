# deploy/.env.example — every variable the compose stack reads, with empty placeholders.
#
# This file is committed; deploy/.env is not (see .gitignore). scripts/install.sh generates
# deploy/.env for a real deployment — do not hand-write secrets into a copy of this file and commit
# it.

# How this server is reached from the internet. Written by deploy/install.sh, which asks once and
# then remembers the answer.
#
#   direct  — this machine has a public IP and ports 80/443 reach it. Caddy publishes both and
#             obtains its own Let's Encrypt certificate. The default.
#   tunnel  — Cloudflare Tunnel. Nothing is published to the host and no inbound port is opened;
#             cloudflared connects outbound to Cloudflare. Adds deploy/docker-compose.tunnel.yml as
#             a second -f, and needs CLOUDFLARE_TUNNEL_TOKEN below.
DEPLOY_MODE=direct

# Public hostname where this stack is reached, e.g. docs.example.com.
#
# In direct mode Caddy requests and renews a TLS certificate for exactly this name — DNS must already
# point at the server before the first `docker compose up`, or certificate issuance fails.
#
# In tunnel mode the public certificate is Cloudflare's, and this must be the public hostname you
# routed to the tunnel (Cloudflare's dashboard calls it the tunnel's "public hostname"). Caddy still
# issues a certificate for the name, but from its own internal CA and only for traffic on the compose
# bridge; the app container is given that CA to trust.
APP_HOSTNAME=

# Cloudflare Tunnel connector token — ONLY used when DEPLOY_MODE=tunnel; leave empty otherwise.
#
# Where it comes from: one.dash.cloudflare.com > Networks > Tunnels > Create a tunnel > Cloudflared.
# Cloudflare shows an install command containing `--token eyJ...`; the token is that value. Route the
# tunnel's public hostname to the service `http://caddy:80`.
#
# This is a SECRET — it is a bearer credential for the tunnel, and anyone holding it can connect a
# connector of their own. It belongs in deploy/.env (mode 600, gitignored) and nowhere else. This
# repository is public.
CLOUDFLARE_TUNNEL_TOKEN=

# Password for the Postgres superuser and the internal service roles the images create
# (supabase_auth_admin, supabase_storage_admin, authenticator). Generated by scripts/install.sh.
POSTGRES_PASSWORD=

# Database name inside the Postgres container. Leave empty to use the image's default ("postgres").
POSTGRES_DB=

# Signing secret for every auth/rest/storage JWT. This is the single most valuable secret on the
# server — anyone holding it can mint a service_role token and read every client's documentation.
# Generated by scripts/install.sh; never hand-write it, never commit a real value.
#
# Slice 2: also consumed directly by the app container (as SUPABASE_JWT_SECRET), which mints
# short-lived app_tenant tokens with it — see deploy/docker-compose.yml.
JWT_SECRET=

# JWTs signed with JWT_SECRET carrying role: anon and role: service_role respectively. Generated by
# scripts/install.sh alongside JWT_SECRET — hand-writing them is where self-hosting usually goes
# wrong.
ANON_KEY=
SERVICE_ROLE_KEY=

# How long an issued JWT stays valid, in seconds. Leave empty for the default of 3600 (one hour).
JWT_EXPIRY=

# Device Wizard's Gemini free-tier vision calls (Google AI Studio). Server-side only.
GEMINI_API_KEY=

# Transactional email — member invites, password recovery, one-time device-approval codes. Leave
# all five empty to run without outgoing mail: sendEmail() then only logs to the console in
# development and silently refuses in production.
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=

# OAuth sign-in. Create these in Google Cloud Console / an Azure app registration. Leave unset to
# run with password sign-in only — the Google and Microsoft buttons then say they are not
# configured.
# Set to true ONLY once the client id and secret below are filled in. GoTrue exits fatally at
# startup if a provider is enabled but incompletely configured.
SUPABASE_AUTH_GOOGLE_ENABLED=
SUPABASE_AUTH_GOOGLE_CLIENT_ID=
SUPABASE_AUTH_GOOGLE_SECRET=
SUPABASE_AUTH_AZURE_ENABLED=
SUPABASE_AUTH_AZURE_CLIENT_ID=
SUPABASE_AUTH_AZURE_SECRET=

# Twilio, for confirming a member's phone number by text. Leave unset to run without SMS — the
# Verify button then says text confirmation isn't set up.
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_FROM_NUMBER=
